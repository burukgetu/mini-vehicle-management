{
  "name": "mini-vehicle-management",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm start",
    "dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongoose": "^8.9.0"
  },
  "devDependencies": {
    "vercel": "^28.1.0"
  }
}
